#include "main.h"
/**
*mul - multiplies 2 numbers
*Return: returns 1 if char is uppercase and 0 otherwise
*@a: char to be multiplied with b
*@b: char to be multiplied with a
*/
int mul(int a, int b)
{
	return (a * b);
}
